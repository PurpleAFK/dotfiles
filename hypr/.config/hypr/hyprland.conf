#############################
### ENVIRONMENT VARIABLES ###
#############################

# Toolkit & Wayland Fixes
env = XDG_SESSION_TYPE,wayland
env = GDK_BACKEND,wayland,x11,*
env = QT_QPA_PLATFORM,wayland;xcb
# env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_QPA_PLATFORMTHEME,kde

# NVIDIA Specifics (Crucial for your GTX 1650)
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = NVD_BACKEND,direct

# Flicker Fix for Electron (Obsidian, Discord, VS Code)
env = ELECTRON_OZONE_PLATFORM_HINT,auto

# Cursor (Hyprcursor is the modern standard)
env = HYPRCURSOR_THEME,Nordzy-catppuccin-mocha-lavender
env = HYPRCURSOR_SIZE,18

exec-once = hyprctl setcursor Nordzy-catppuccin-mocha-lavender 18 

exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'Nordzy-catppuccin-mocha-lavender'
exec-once = gsettings set org.gnome.desktop.interface cursor-size 18

exec-once = easyeffects --gapplication-service

env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
# cursor {
#     no_hardware_cursors = true
#     enable_hyprcursor = true
#     warp_on_change_workspace = true
# }
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
source = ~/.config/hypr/colors.conf
source = ~/.cache/wal/colors-hypr.conf

################
### MONITORS ###
################
monitor = eDP-1, 1920x1080@144, 0x0, 1
monitor = DP-2, disabled

workspace = 1,monitor:eDP-1
workspace = 2,monitor:eDP-1
workspace = 3,monitor:eDP-1
workspace = 4,monitor:eDP-1
workspace = 5,monitor:eDP-1
workspace = 6,monitor:eDP-1
workspace = 7,monitor:eDP-1
workspace = 8,monitor:DP-1
workspace = 9,monitor:DP-1
workspace = 10,monitor:DP-1


###################
### MY PROGRAMS ###
###################
#$themes = bash ~/Documents/themes/theme-switcher.sh
$terminal = kitty
$fileManager = thunar
# $menu = rofi -show drun
$menu = rofi -show drun -theme ~/.config/rofi/menu.rasi
$powermenu = zsh ~/.config/rofi/powermenu.sh
$screenshot = hyprshot -m output -m active --clipboard-only
$screenshot1 = hyprshot -m region --clipboard-only --freeze
$hyprlock = hyprlock
$killpanel = killall waybar
$startpanel = waybar
$notes = flatpak run md.obsidian.Obsidian

#################
### AUTOSTART ###
#################
# exec-once=hyprpaper
exec-once = swww-daemon 
exec-once = ~/.config/hypr/scripts/wallpapers.sh startup
# exec-once=waybar

# Load the last set wallpaper on startup
# This command sources a script that sets the wallpaper and applies Pywal colors
#exec-once= ~/.config/hypr/set_startup_wallpaper.sh

# Restore Pywal colors for other applications (like the terminal) on startup
#exec-once = wal -R

# Layer rules to stop animations from being captured
layerrule = match:namespace selection, animation off
layerrule = match:namespace slurp, animation off

###################
### KEYBINDINGS ###
###################
$mainMod = SUPER 

#scripts
# bind = $mainMod SHIFT, W, exec, wofi --show files "$HOME/Pictures/Wallpapers" | xargs ~/bin/set_wal_and_kitty.sh
# bind = $mainMod, W, exec, ~/.config/scripts/wallpaper-chooser.sh
bind = alt, W, exec, ~/.config/hypr/scripts/wal-set.sh

# Volume control with on-screen notifications
bindel=, XF86AudioRaiseVolume, exec, ~/.config/hypr/scripts/volume.sh up
bindel=, XF86AudioLowerVolume, exec, ~/.config/hypr/scripts/volume.sh down
bindl=, XF86AudioMute, exec, ~/.config/hypr/scripts/volume.sh mute

#sway NC
bind = SUPER, n, exec, swaync-client -t

bind = alt, return, exec, $terminal
bind = alt, Q, killactive,
bind = $mainMod, M, exit
bind = alt, i, pin
bind = alt, F, fullscreen
bind = alt, b, exec, helium file:///home/purpleafk/dotfiles/helium/startpage.html
bind = $mainMod SHIFT, A, exec, ~/bin/anki-sync.sh
bind = $mainMod, W, exec, kitty --class floating -e ~/.config/hypr/scripts/wifi-menu.sh
bind = ALT, t, exec, kitty --class todo_scratchpad nvim ~/notes/todo.md
bind = ALT, o, exec, ~/.config/scripts/todoist-add.sh
bind = $mainMod, E, exec, $fileManager
bind = alt, N, exec, ~/.config/scripts/rofi-notes.sh
bind = alt, R, exec, $notes
bind = $mainMod, F, togglefloating,
bind = alt, D, exec, $menu
bind = alt, Z, exec, $powermenu
bind = alt, S, layoutmsg, togglesplit, 
bind = alt, H, movefocus, l
bind = alt, L, movefocus, r
bind = alt, K, movefocus, u
bind = alt, J, movefocus, d
bind = $mainMod, P, exec, $screenshot
bind = alt, P, exec, $screenshot1
bind = , Print, exec, $screenshot1
bind = $mainMod, L, exec, $hyprlock
bind = $mainMod, K, exec, $killpanel
bind = $mainMod, J, exec, $startpanel
bind = alt, 1, workspace, 1
bind = alt, 2, workspace, 2
bind = alt, 3, workspace, 3
bind = alt, 4, workspace, 4
bind = alt, 5, workspace, 5
bind = alt, 6, workspace, 6
bind = alt, 7, workspace, 7
bind = alt, 8, workspace, 8
bind = alt, 9, workspace, 9
# bind = ctrl, 1, workspace, 6
# bind = ctrl, 2, workspace, 7
# bind = ctrl, 3, workspace, 8
# bind = ctrl, 4, workspace, 9
# bind = ctrl, 5, workspace, 10
bind = $mainMod, 1, movetoworkspace, 1
bind = $mainMod, 2, movetoworkspace, 2
bind = $mainMod, 3, movetoworkspace, 3
bind = $mainMod, 4, movetoworkspace, 4
bind = $mainMod, 5, movetoworkspace, 5
bind = $mainMod, 6, movetoworkspace, 6
bind = $mainMod, 7, movetoworkspace, 7
bind = $mainMod, 8, movetoworkspace, 8
bind = $mainMod, mouse_up, workspace, e+1
bind = $mainMod, mouse_down, workspace, e-1
bindm = alt, mouse:272, movewindow
bindm = alt, mouse:273, resizewindow
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

bindl = , switch:on:Lid Switch, exec, hyprctl dispatch dpms off eDP-1
bindl = , switch:off:Lid Switch, exec, hyprctl dispatch dpms on eDP-1

#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 5
    gaps_out = 15
    border_size = 2
    resize_on_border = false
    allow_tearing = false
    layout = master
    col.active_border = $color1
}

decoration {
    rounding = 0
    active_opacity = 1
    inactive_opacity = 1
    shadow {
        enabled = false
        range = 25
        render_power = 1000
        # color = rgb(689d6a)
        # color = rgb(ebdbb2)
    }
    blur {
        enabled = true
        size = 1
        passes = 5

        vibrancy = 0.1696
    }
}
animations {
    enabled = yes, please :)
        bezier = wind, 0.05, 0.9, 0.1, 1.05
        bezier = winIn, 0.1, 1.1, 0.1, 1.1
        bezier = winOut, 0.3, -0.3, 0, 1
        bezier = liner, 1, 1, 1, 1
        bezier = overshot, 0.05, 0.9, 0.1, 1.05
        bezier = smoothOut, 0.5, 0, 0.99, 0.99
        bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
        bezier = easeOutQuint,0.23,1,0.32,1
        bezier = easeInOutCubic,0.65,0.05,0.36,1 
        bezier = linear,0,0,1,1
        bezier = almostLinear,0.5,0.5,0.75,1.0
        bezier = quick,0.15,0,0.1,1
        animation = windows, 1, 6, wind, slide
        animation = windowsIn, 1, 5, winIn, slide
        animation = windowsOut, 1, 3, smoothOut, slide
        animation = windowsMove, 1, 5, wind, slide
        animation = border, 1, 1, liner
        animation = borderangle, 1, 180, liner, loop 
        animation = fade, 1, 3, smoothOut
        animation = workspaces, 1, 5, overshot
        animation = workspacesIn, 1, 5, winIn, slide
        animation = workspacesOut, 1, 5, winOut, slide
        animation = layers, 1, 3.81, easeOutQuint 
        animation = layersIn, 1, 4, easeOutQuint, popin 50%
        animation = layersOut, 1, 3, easeOutQuint, slide
}

dwindle {
    preserve_split = true
}

# master {
#     new_status = master
# }

misc {
    force_default_wallpaper = -1
    disable_hyprland_logo = false
}

#############
### INPUT ###
#############

input {
    kb_layout = us,br
    kb_variant =
    kb_model =
    kb_options = grp:ctrl_space_toggle
    kb_rules =
    accel_profile = flat
    follow_mouse = 1
    sensitivity = 0.8 
    touchpad {
        natural_scroll = false
    }
}

# gestures {
#     workspace_swipe = false
# }

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


##############################
### WINDOWS AND WORKSPACES ###
##############################

# Zathura Rules
windowrule = match:class ^(zathura)$, float on
windowrule = match:class ^(zathura)$, size 1000 1200
windowrule = match:class ^(zathura)$, center on
windowrule = match:class ^(zathura)$, workspace 3
windowrule = match:class ^(zathura)$, opacity 0.95 0.90

# Todo Scratchpad
windowrule = match:class ^(todo_scratchpad)$, float on
windowrule = match:class ^(todo_scratchpad)$, center on
windowrule = match:class ^(todo_scratchpad)$, size 650 80%
